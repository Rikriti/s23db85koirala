extends layout

block content
  h1= title

  - var image_names = ["africanEl.jpg","brazilEl.jpg","mammoth.jpg","srilankaEl.jpg","whiteEl.jpg"];
  - for(i=0;i<3;i++) {
     - var r = Math.floor(Math.random()*5) ;
     img(src=`./images/${image_names[r]}` alt=`img${image_names[r]}`)

  -}